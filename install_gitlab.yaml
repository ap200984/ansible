---
- hosts: all
  become: true
  vars:
    docker_network_name: docker-net
    docker_network_subnet: 172.20.0.0/16
    docker_network_range: 172.20.240.0/20

  tasks:
    # - name: Include secrets
    #   include_vars: secrets/common_secrets.yml

    - name: Create a network for gitlab
      docker_network:
        name: "{{ docker_network_name }}"
        ipam_config:
          - subnet: "{{ docker_network_subnet }}"
            iprange: "{{ docker_network_range }}"

    - name: Ensure /tmp/devnull directory exists
      file:
        path: /tmp/devnull
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Create null device at /tmp/devnull/null
      command: mknod /tmp/devnull/null c 1 3
      args:
        creates: /tmp/devnull/null

    - name: Install gitlab
      community.docker.docker_container:
        name: gitlab
        image: "gitlab/gitlab-ce"
        shm_size: 256m
        memory: 4g
        memory_swap: 6g
        networks:
          - name: "{{ docker_network_name }}"
        ports:
          - 1080:80/tcp
          - 1022:22/tcp
        volumes:
          - /gitlab/config:/etc/gitlab
          - /var/log/gitlab:/var/log/gitlab
          # - /tmp/devnull/null:/var/log/gitlab/sidekiq/current
          - /gitlab/data:/var/opt/gitlab
        env:
          GITLAB_OMNIBUS_CONFIG: |
            external_url 'https://gitlab.{{ domain }}/';
            gitlab_rails['gitlab_shell_ssh_host'] = 'gitlab.{{ domain }}';
            gitlab_rails['gitlab_shell_ssh_port'] = 1022;

            nginx['listen_port'] = 80;
            nginx['listen_https'] = false;
            nginx['proxy_set_headers'] = {
              'X-Forwarded-Proto' => 'https'
            };

            puma['worker_processes'] = 0;
            puma['per_worker_max_memory_mb'] = 700;
            puma['min_threads'] = 1;
            puma['max_threads'] = 4;

            sidekiq['concurrency'] = 5;
            sidekiq['log_level'] = 'fatal';

            prometheus_monitoring['enable'] = false;
            prometheus['enable'] = false;
            alertmanager['enable'] = false;
            node_exporter['enable'] = false;
            redis_exporter['enable'] = false;
            postgres_exporter['enable'] = false;
            gitlab_exporter['enable'] = false;

            gitlab_rails['env'] = {
              'MALLOC_CONF' => 'dirty_decay_ms:1000,muzzy_decay_ms:1000'
            };

            gitaly['env'] = {
              'MALLOC_CONF' => 'dirty_decay_ms:1000,muzzy_decay_ms:1000',
              'GITALY_COMMAND_SPAWN_MAX_PARALLEL' => '2'
            };
        state: started
        restart_policy: unless-stopped
        log_driver: "none"

    - name: Install GitLab Runner
      community.docker.docker_container:
        name: gitlab-runner
        image: "gitlab/gitlab-runner:latest"
        volumes:
          - /gitlab/gitlab-runner:/etc/gitlab-runner
          - /var/run/docker.sock:/var/run/docker.sock
        state: started
        restart_policy: unless-stopped