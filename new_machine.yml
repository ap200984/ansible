---
- name: Configure new machine
  hosts: "{{ hosts | default('all') }}"
  become: true
  gather_facts: true

  roles:
    - tz
    - users
    - python3
    - iptables
    - utils
    - ntp_client
    - rsyslog
    - docker
    - zabbix_agent
    - ssh_keys

